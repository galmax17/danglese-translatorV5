import React from 'react';
import ReactDOM from 'react-dom/client';
import DangleseTranslator from './components/DangleseTranslator';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <DangleseTranslator />
  </React.StrictMode>
);
